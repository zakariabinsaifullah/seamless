# Taste
- Wants new code to follow the existing codebase style and conventions ("follow my existing codebase style"). Confidence: 0.9
- Prefers reusing design tokens from theme.json via their CSS custom properties (e.g., `var(--wp--preset--gradient--btcpa-gradient-one)`) rather than hardcoding the same gradient/color again in component CSS — refers to gradients by exact token name (e.g., "btcpa-button-gradient") and expects the agent to map that to the theme's CSS variable; e.g., asked to "add button gradient as background color" to a new element (the post-excerpt more-link), meaning reuse the existing button gradient token. When told to "use button [background] from @theme.json", the intended treatment is the `btcpa-button-gradient` preset as a full gradient background fill with white text — NOT the white-fill + gradient-border two-layer technique (tried twice and rejected as "wrong"); the user corrected the agent by naming the exact gradient token to use. Confidence: 0.9
- Prefers CSS-drawn icons/arrows over SVG or image assets when possible — asked "can we use CSS code to make the arrow [the] share or will I share svg?" rather than assuming an asset must be supplied; an `::after` chevron made from rotated borders satisfies this. Confidence: 0.5
- When the user does supply the exact SVG markup for an icon (pasted inline, e.g., an 11×11 white-stroke arrow with 1.5px round linecaps), expects it reproduced verbatim — dimensions, stroke width, linecap/linejoin, and color — rather than approximated with a CSS-drawn chevron; embedding it as a data-URI background on `::after` (no separate asset file) and keeping only the hover nudge (translateX) as an enhancement matches this. Confidence: 0.65
- Prefers implementing features as extensions of core blocks (e.g., core/paragraph, core/heading) rather than custom blocks — asked for a responsive-alignment extension and a Highlight extension. Confidence: 0.6
- Likes responsive/breakpoint-aware settings for spacing and gap attributes in blocks (e.g., responsive item-gap and icon-gap controls). Confidence: 0.6
- Wants block layout behaviors (e.g., the icon's vertical alignment relative to its title) exposed as user-facing settings in the block inspector — a select/toggle in the relevant panel wired to a block attribute — rather than fixed in CSS; and expects such settings to be conditional, shown only when the related feature they affect is enabled (e.g., the vertical-alignment control appears in the List Title panel only when "Add List Title" is on). Confidence: 0.65
- Wants mobile/responsive variants for interactive component layouts — e.g., filter/category tab buttons should become full-width and stack vertically with explicit inner padding (e.g., 16px) on the mobile breakpoint, rather than shrinking in place. Confidence: 0.55
- When rebranding or renaming across a project, expects the change applied consistently everywhere in the theme, not partially. Confidence: 0.7
- Ports features from a bundled reference theme (e.g., `ref/`) into the current theme rather than rewriting from scratch, expecting the port to be renamed to the current theme's brand and prefix (e.g., `pandora_*` → `btcpa_*`). Confidence: 0.75
- When asked to redesign something "like" a reference file (e.g., an admin page "like @ref/inc/shortcodes-reference.php"), expects the redesign to faithfully mirror the reference's structure and visual design — header with logo/title, stacked cards with badges, example code + copy button, attribute grid columns, footer tip — adapted only by renaming to the theme's brand and prefix, not by inventing a new layout. Confidence: 0.7
- Explicitly scopes ports to only the features wanted — e.g., "skip team related shortcode" — and expects the skipped feature to be removed entirely (no leftover created files or references), not partially built. Confidence: 0.55
- Wants the agent to ask for clarification when it encounters issues or ambiguity during a task, rather than guessing or silently deciding — signals this explicitly and repeatedly ("If you find any issues, ASK me", "If you have any questions ASK me"). Confidence: 0.95
- When the user explicitly authorizes fixing (e.g., "check my ... file and if find any issues fix"), wants issues fixed directly and reported, not met with clarification questions — the ask-vs-fix behavior is signaled per task. Confidence: 0.5
- Communicates in terse, numbered/bulleted task lists with direct imperatives. Confidence: 0.85
- Specifies exact visual parameters when requesting design changes (e.g., "a bottom 2px underline text decoration with 5px offset", "make the width for the ID column 100px max", "1px solid #DDE2F0 border with 16px inner padding + 20px border radius", "make sure the metas font size is 14px", "column gap: 16px and row gap: 32px", "24px font size ... 1.4 line-height, color: #19191B", "make the name + designation a group with 0 inner gap, but among image + name/designation group + short intro the inner gap is 16px") rather than describing intent vaguely. Confidence: 0.97
- For people/team photos shown in cards, expects them cropped to a fixed aspect ratio (e.g., 16:9) with `object-position: top` so faces/heads stay in frame — "16:9 ratio with top position so that the head looks fine". Confidence: 0.6
- References code locations by @-mentioning file/directory paths (e.g., "@src/blocks/timeline/", "@inc/form.php", "@ref/inc/shortcodes-reference.php") rather than describing them in prose. Confidence: 0.7
- Prefers per-item spacing (e.g., `padding-bottom` on each item) over `flex gap` on the parent container when each item carries its own connector/`::before` styling, and expects the last item to drop the connector. Confidence: 0.5
- Keeps design style variants visually consistent with the base/default style — e.g., the Alternative button should reuse the default button's gradient for its border, changing only the specified attribute (transparent background) rather than introducing new colors. Confidence: 0.8
- Applies design changes to every variant of a component together (e.g., a hover change requested "for default and alternative button styles" should cover both, not just the primary one). Confidence: 0.7
- Prefers minimal, single-property hover states — on hover everything stays identical except the one specified change (e.g., only the gradient direction flips, keeping the same gradient colors/stops — "just change the gradient angle but keep the same gradient background"), with no other recoloring, movement, or fading. Confidence: 0.9
- Wants hover/state changes to animate smoothly rather than snap — explicitly asks for a short 0.2s transition (e.g., "add a 0.2s transition to make the change smooth") and expects it applied consistently to all affected variants. Confidence: 0.7
- Gradient-border effects on transparent elements must render the gradient only as the border ring, never as the whole element background; if a background layer is required for the technique, it must be transparent (opacity 0) so the fill stays see-through. Confidence: 0.8
- Hover-only effects must be guaranteed absent in the normal state — e.g., a hover gradient border should be defined only inside the `:hover` rule (so it doesn't exist at all until hover) rather than hidden via opacity, which could be overridden or leak. Confidence: 0.7
- Works directly on the main branch and expects all pending working changes to be committed and pushed straight to main (no feature branches or PR workflow) — a bare "push on main" instruction means commit everything (all pending working changes) and push to main in one go; repeated again with a bare "push on main" covering a large set of accumulated uncommitted work (rebrand + CPT + shortcodes + pre-existing changes). Confidence: 0.98
- Wants local tooling/config directories (e.g., `.commandcode`, alongside existing `.claude`) excluded from distribution builds via `.distignore`, so local-only files never ship in the theme zip — explicitly asked to "add .commandcode in .distignore". Confidence: 0.7
- Wants an "ID" column on admin list-table screens for quick item reference — added to both the Categories screen and the Team Members screen — and expects the column width capped with a small fixed max (e.g., "100px max", "max 70px width") so the table stays compact. Confidence: 0.65
- Dislikes auto-scroll on filter/tab clicks — clicking a category tab should update the grid in place without moving the viewport (reported as "it scrolls me to top always" caused by `scrollIntoView` in the filter handler). Confidence: 0.7
- Uses a fixed brand color convention: `#F47149` (orange) as the accent color for interactive/hover/active states (e.g., title hover, read-more links, active/hover category tabs), `#080046` (deep navy) as the default color for headings/titles, and `#551C25` as the brand burgundy for team member designations. Confidence: 0.7
- Reuses the same design technique and state styling across components — e.g., a hover gradient border on post cards should replicate exactly what the kadence section already does ("like as kadence section, we already added"), using the same gradient and masked-ring technique rather than introducing a new gradient or look; likewise a component's active state should match another component's active state ("make the active pagination color like as active category tab" → same `#F47149` + white scheme). Confidence: 0.85
- Wants interactive UI elements like filter/category tabs to use a normal font-weight of 400 (no bolding) and will explicitly ask to verify it ("make sure the tabs font weight is 400"). Confidence: 0.6
 interactive elements (e.g., category tabs) as identical — states requested together ("on hover and in case of active") should share the same colors (white text on `#F47149`), and unspecified hover details are inferred from the active state. Confidence: 0.6
- Prefers applying a custom design directly to the default/core element rather than shipping it as a separately-selectable block style variant — when the outline-link style wasn't visible/required manual selection, asked to "remove outline style, rather apply the style to the default one": remove the block style registration and target the default selector (`.wp-block-post-excerpt__more-link`) so the styling applies globally without picking a style. Confidence: 0.7
- Wants Gravity Forms fields taller, but rejects any technique that breaks the native select or imposes rigid sizing — after asking to "increase the height of gravity fields not using min-height", successively rejected `min-height`, then `height` ("no height"), then vertical `padding` ("sorry doesn't work, remove padding, keep it default"); the height-increase goal persists though, as the user later asked "is there any better way to increase the height so that select field works perfectly" — any proposed technique must keep the select's native arrow/behavior intact, avoid `min-height`/`height`/`padding` sizing, and stay overridable. Confidence: 0.9
- Prefers `background-color` over the `background` shorthand when overriding element backgrounds, so existing `background-image`s are preserved — Gravity Forms' native select dropdown arrow (rendered via `appearance: none` + background-image) disappeared because the shared field rule reset it with `background: #ffffff !important`; switching to `background-color: #ffffff !important` restored the arrow. Confidence: 0.8
ain slug, and code prefix in both casing variants (e.g., "Theme: Han Group, Text Domain: han-group, Prefix: hang/HANG") — rather than describing the changes in prose, and expects each token applied to its own layer (display text, handles/text domains, function/class/JS prefixes, uppercase constants). Confidence: 0.6
- Prefers full prefix consistency over backward compatibility during a rebrand — when asked whether renaming shortcode tags (e.g., [btcpa_posts_grid] → [hang_posts_grid]) would break previously authored content, chose the full rename so the theme prefix stays consistent. Confidence: 0.5
- For content types that explicitly won't have a front-end single view (e.g., a "Team" post type), expects them registered as non-public — `publicly_queryable => false`, `exclude_from_search => true`, `rewrite => false`, `query_var => false` — while keeping `show_in_rest => true` so the block editor still works; content stays queryable via WP_Query for grid blocks. Confidence: 0.5
- Prefers structured custom meta fields over the block editor's content area for profile-style post types (e.g., team members) — explicitly asked to remove `editor` support and instead add another meta field ("Short Intro") per member, extending the existing meta box/save-handler pattern rather than letting editors write free-form content; also strips the remaining editor surface, asking to "remove support of excerpt, slug etc" so the post type keeps only title + thumbnail. Confidence: 0.7
- Prefers admin meta boxes for content-type details (e.g., "Team Details") to render in the main column under the title (`normal` context) rather than in the right sidebar (`side` context) — asked to "keep the Team Details under the title instead of at the right side position". Confidence: 0.6
- Wants listing shortcodes to support an `ids` attribute for rendering specific posts by ID, with an empty value falling back to ALL items in descending (newest-first) order rather than alphabetical/ASC — for `[team_grid]`, `ids=""` should render every team member newest-first (`order => DESC`, `orderby => date` as defaults); confirmed when the user reported "no team members found" with 4 members present, expecting the default (empty `ids`) shortcode to render all of them. Confidence: 0.7
- When a shortcode receives an explicit list of post IDs, expects those posts rendered in exactly the order written (via `post__in` + `orderby => post__in`), not re-sorted by the shortcode's default order. Confidence: 0.5
- Uses `!important` on explicitly-specified design styles so they win over conflicting theme styles rather than relying on specificity — asked to "make the !important for designation color" after setting the designation to `#551C25`, then extended it to "!important for designation all styles" (margin, font-family, font-size, font-weight, line-height, color all get `!important`), so when a component's full style set is explicitly specified, the user expects `!important` applied broadly across all of its properties, not just one — then asked to do the same for the sibling `.htg-card__intro` styles ("similarly add !important for all htg-card__intro style"), showing the treatment is expected to extend consistently to sibling components, not just the one initially styled. Confidence: 0.75
- Prefers a smooth hover zoom-in effect on card images triggered by hovering the whole card/container (not just the image) — implemented with a scale transform + transition on the `img` (e.g., `scale(1.05)` over ~0.4s with an easing curve), clipped by the photo wrapper's existing `overflow: hidden` so the zoom stays inside the rounded photo area. Confidence: 0.6
- Avoids hardcoding `font-family` in component CSS when the theme font applies automatically — asked to remove an explicit font-family declaration from designation styles ("remove font family since it will get applied automatically") so the element inherits the theme's default font rather than restating it. Confidence: 0.6
- When a layout bug persists after a fix, a browser-cache explanation does not satisfy the user — they re-report the exact same symptom again rather than accepting "hard refresh" (after being told the stale `?ver=1.0.0` stylesheet was the cause, the user came back with "used this [team_grid] getting 2 columns while I have 4 team members"), so the agent must keep hunting for a real code-level root cause (e.g., missing `box-sizing: border-box`) instead of attributing the issue to caching. Confidence: 0.6
- Wants incomplete/final rows of card grids to be horizontally centered rather than left-aligned — e.g., a team grid with 4 or 5 items across 3 columns should have the leftover items in the last row centered; implemented via a wrapping flex layout with `justify-content: center` (full rows still fill edge to edge) while keeping the column count working through a CSS variable. The configured column count must be preserved across breakpoints (e.g., 3 columns on desktop AND tablet), collapsing only to 1 column on small mobile (≤767px) — an intermediate 2-column drop at ≤1024px was explicitly rejected as wrong ("not making it 2 columns, it will be 3 columns"), and the user re-reported "still it shows 2 columns" after the fix, reaffirming the 3-column expectation; the user later re-reported the same symptom yet again via `[team_grid]` with 4 members ("getting 2 columns while I have 4 team members"), and the true root cause turned out to be the theme lacking a global `box-sizing: border-box` — under default `content-box` sizing the calc'd card widths overflowed and wrapped at 2 per row, fixed by setting `box-sizing: border-box` on the component itself. Confidence: 0.85
